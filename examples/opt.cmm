two = 1 + 1;

if (two < 0) {
  two2 = two;
  while (two2 < 0) {
    two2 = two2 + 1;
  }
}

n = arg;

r = 1;

if ((n < 0) || (n == 0)) {
  return r;
}

while ((n > 0) || (n > 0)) {
  one = 1;
  three = one + two;
  four = three + one;
  ndiv = n / two;
  nmdiv = (n - one) / two;
  newn = n;
  if (ndiv == nmdiv) {
    newn = (n - one) / two;
  } else {
    newn = n / two;
  }
  n = newn;
  r = r + one;
}

return r;
